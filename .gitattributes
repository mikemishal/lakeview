# Normalize line endings to LF in the repository for all text files.
# This prevents the whole tree showing as modified when checked out on Windows.
* text=auto eol=lf

# Treat known binaries as binary.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.zip binary
